# 书库工作台：业务需求与决策记录

> 用途：让新的 Agent 或网页端对话在不重读全部历史聊天的情况下，理解这个项目的真实业务、设计原因和已经变更过的要求。

## 1. 这不是什么

这不是通用人力资源系统，也不是要记录每一次代班、每一次签到行为的全能管理工具。

它只服务于两类最终交付：

1. 可以人工修改、自动校验的排班表。
2. 以纸质签到统计为依据的工时一览、工时差额、人工转账配平和公示表。

项目的主线始终是：

```text
输入已确认数据 → 固定脚本计算 → 表格预览 → 负责人修改 → 自动校验 → 输出 Excel
```

## 2. 为什么这样设计

早期尝试过把人名、班次和自然语言要求整段交给大模型排班或计算。实际出现过：

- 同一个人被当成两个人，在同一班次重复出现。
- 模型产生名单外的人名，或在长文中混淆姓名。
- 对“自由安排”“尽量”“可以少排”等表达理解过度，把想法当成硬规则。
- 工时求和、名额分配和转账配平耗时长，过程不稳定，还要人工重算。
- 对话中的临时假设容易被继续当成不变的业务规则。

因此，大模型可以帮助整理自然语言、解释错误和提出草案，但不是姓名、算术和最终校验的唯一依据。

## 3. 责任分工

### 固定脚本必须负责

- 姓名唯一性、名单内外检查和行对应。
- 班次人数、时长、工时求和、差额计算。
- 同一班次不能出现重复人名。
- 硬约束违反、缺岗、超员和未配平检查。
- 转账收付求和、方向和公示表正负号。
- 最终 Excel 生成；正式导出前必须在服务端重算和重新校验。

### AI 适合负责

- 把用户的自然语言整理成“待确认的结构化草案”。
- 区分硬约束、软偏好、分配方案和临时想法。
- 解释为什么某个排班无解、为什么转账还没有配平。
- 给出可选方案，但不擅自把未确认想法写入正式规则。

### 负责人必须确认

- 老师确认的排班周期、班次、每班人数和特殊日期例外。
- 本学期、本阶段或本周的实际人员名单。
- 自然语言约束最终属于哪一类。
- 对无法绝对平均的工时怎样取舍，是否减少某一个岗位。
- 纸质签到表统计出的实际到岗工时。
- 本次官方下发工时上限和每个人的实际下发工时。
- 组长奖励、外部差额等特殊情况怎么处理。
- 草稿的人工修改和最终发布。

## 4. 排班表的已确认需求

### 输入

- 具体开始日期和结束日期，不假定永远是七天。
- 一个或多个班次：编号、名称、开始时间、结束时间或时长、默认人数。
- 某些日期可以临时使用不同人数。
- 人员可以是 10–40 人左右的随机名单，不能把人数写死。
- 名单可以按学期或阶段保存，但每个新周期仍需确认实际参加者。
- 硬约束的稳定形式是“人员 × 日期 × 班次”勾选表：勾选表示绝对不能排，空白表示可排。
- 高级文本只接受 `姓名|YYYY-MM-DD|班次编号`，用于粘贴已经人工确认的结构化结果。
- 工时分配口径可选：按全部班次总工时平均；或按 2 小时班、3 小时班等班岗时长分别平衡。
- 个人目标工时是可选输入，未填人员继续使用自动平均目标，不能被当成 0 小时。

### 硬性校验

- 同一个人在同一班次只能出现一次。
- 同一个人可以在同一天不同班次出现，不能因此误判为重复。
- 人名必须属于本周期名单。
- 明确勾选了不可排的人不能出现在该日期该班次。
- 每班实际人数必须与当日需求一致；无解时必须显示缺岗，不得伪造名字填满。
- 输出前再次校验人数、姓名、硬约束和工时求和。

### 软偏好

“优先晚班”“尽量集中在同一天”“尽量与某人一起”“可以为了均衡少排”等属于软偏好。当前可以由 AI 整理、由人工在草稿中调整，不得在未确认时自动升级为硬约束。

## 5. 工时与转账表的已确认需求

### 输入和计算

- 统计时段是可编辑的具体开始日期和结束日期，不能只按“某月”固定处理。
- 本次名单重新确认，可以与上次不同。
- 纸质签到先由负责人统计为“姓名 + 实际工时”。页面支持逐个修改、按名单顺序竖排粘贴工时，或上传已统计表格。
- 输入错误必须可以回退和修改。
- 先输出一份“实际工时一览表”，包含逐人工时和实际总工时，再进入下发与配平。
- 官方下发工时上限每次可改；33、35、40 都只是曾经出现的示例，不是永久规则。
- 每个人的官方下发工时也是独立可编辑数字。个别人实际 28 小时时，可以经确认下发 28 以减少转账；这只是灵活性示例，不是“28 就必须下发 28”。
- 差额公式为：`实际工时 - 官方下发工时`。正数表示应收，负数表示应转出。
- 系统可生成尽量少拆分的配对草案，但官方下发数字的统筹和特殊人情安排保留给负责人。

### 转账方向和输出

- 人工转账输入固定为：`付款人|收款人|工时`，即前面的人给后面的人转。
- 公示表的展示顺序反过来：收款人在第一列并显示正数；付款人在后面列并显示负数。
- 配平尚未完成时，必须明确提醒并允许返回修改。
- 未配平时可以输出带差额的“当前核算草案”，但不能伪装成正式结果。
- 只有完全配平且逐人收付与差额一致时，才能输出正式转账表和公示表。

## 6. 已经改过或放弃的方向

1. **自然语言直接驱动全部排班**：改为勾选硬约束 + 固定计算 + 人工调整。
2. **统一官方下发 33 小时**：已否定。上限可改，逐人下发工时可改。
3. **只按月份做转账表**：已否定。必须使用具体开始和结束日期。
4. **排班自动成为实际出勤**：已否定。实际工时只依据签到统计。
5. **记录每一次代班和“谁替谁”**：暂不做。当前只使用最终实际工时。
6. **工作台自带语音识别**：不做。使用电脑或手机现有的语音输入键盘。
7. **先做飞书、小程序或完整多用户系统**：暂不做。先让负责人一个人的工作流可用。
8. **组长补贴按每次 0.5 小时，或每月固定 2 小时**：仍是待与老师确认的方案，不得写死。
9. **“像游戏一样的网页”**：这是最新的交互和视觉方向，尚未被定义为具体界面规则。它不能改变上述计算、校验和数据责任边界；应先单独做交互原型并让用户确认。

## 7. 交互方面的确认原则

- 只问当前输出所必需的信息，不把简单工作过度电子化。
- 操作顺序和真实业务顺序一致，可以回退修改。
- 输入格式尽量使用表单、勾选和数字空格，不要强迫用户学习复杂语法。
- “输出 Excel”与“选择保存位置”应距离很近；在线版则直接下载到当前设备。
- 页面必须显示计算一览和未完成提醒，不能只在后台产生一个用户看不到的文件。
- 用户的“我在想”“有没有可能”是待讨论想法，不是已确认规则。执行会改变工作流的想法前，必须再确认。

## 8. 网页修改后必须通过的核心验收

### 排班

- 使用一个 30–40 人的随机名单能完成整个流程。
- 班次只需填名称、时间和人数，不会再出现 `shifts must be a non-empty list`这类用户无法理解的死路。
- 硬约束勾选后绝不违反；软偏好没有被偷偷当成硬约束。
- 每个班次人数正确，无重复姓名，无名单外姓名。
- 同一人在不同班次出现时，个人总工时正确累加。
- 按总工时平衡和按班岗时长分别平衡都有可重复测试。
- 人工修改草稿后会重新校验，未通过时不能输出伪正式成品。
- 下载的 Excel 能打开，包含完整班次、每日人数和个人工时汇总。

### 工时与转账

- 姓名竖排粘贴后，同样数量的竖排工时能按行一一对应。
- 名单行数与工时行数不一致时必须报明确错误，不能错位匹配。
- 实际工时和逐人官方下发工时都能反复修改。
- 上限可以改为 40 或其他数字，不再被默认 33/35 卡住。
- 实际总工时、下发总工时、差额、应收和应转出的求和必须相互对得上。
- `付款人|收款人|工时`方向永远不颠倒；公示表正负号和列顺序正确。
- 未配平时可返回修改或输出草案；已配平时能输出正式表和公示表。
- 每一个输出按钮都有用户看得到的下载结果。

## 9. 当前代码边界

- 现有排班器主要处理明确硬约束和工时均衡，尚未实现通用软约束优化器。
- 自然语言整理仍是外部 AI 辅助 + 人工确认，不是运行时必须依赖。
- 现有网页是 Python 服务 + 原生 HTML/CSS/JavaScript，不是纯静态 GitHub Pages 应用。
- 当前是负责人本人或小范围使用工具，不是含多角色权限的学校系统。
- 网页视觉重设计应与计算核心分开：可以重做页面，但不能边改视觉边无测试地改动排班、配平和 Excel 输出逻辑。

## 10. 给新 Agent 的固定续接指令

可以把下面整段粘贴给新对话：

```text
继续 LibSchedPay 书库工作台。请先完整读取 README.md、工程交接说明.md、业务需求与决策记录.md 和当前测试，暂时不要重写代码。

先做三件事：
1. 用你的话复述排班、工时审计和转账的真实工作流。
2. 把你已经复现的每一个网页问题映射到“输入、固定计算、人工修改、自动校验、Excel 输出”中的某一层。
3. 列出你认为本次应修复的最小问题集、不应顺手改动的已稳定功能，以及每一项对应的测试。

确定方案前不要添加新 AI 功能，不要把用户的临时想法当成硬规则。修改时一次只改一层，保留排班、工时、转账和 Excel 的现有回归测试。
```



## 2026-09-05 网页版本补充（优先于旧描述）

用户再次确认：晚班 19:00–22:00；转账只使用工时，不涉及金额；
实际工时来自人工统计的签到，不依赖排班或缺岗；官方下发只有本次上限约束，
每个人可以分别填写。实际工时统计表在进入下发步骤前可独立输出。

原“服务端重新校验”是为了防止旧预览或人工编辑绕过校验，并非必须部署 Python HTTP 服务。
公开版由浏览器 Worker 内的原 Python 代码在导出前重新校验；本地服务沿用同一导出边界。
没有引入由 AI 承担姓名匹配、算术、硬约束或最终配平的设计。

修复范围、四项问题的五层映射和回归证据见《网页版本交付与回归记录.md》。


GitHub 复现入口已补充：`scripts/download_pyodide.py` → `scripts/stage_browser.py`；详见《部署与GitHub说明.md》。原计算流程未变。
# 2026-09-05 补充：整周期工时公平与可复现源码

本节记录用户在实际试用公开网页后的新确认，优先于旧版对分配口径的描述：

- 保留两种口径。第一种只追求总工时尽量相同；第二种也先追求总工时，再尽量平均每类时长的班岗次数。早班与下午班都是 2h，应合为一类；晚班 19:00–22:00 为 3h。
- 所有人不可排的格子必须参与整周期计算，不能为工时平均而违反禁排。不能把旧算法没找到等工时误认为约束导致不可能。
- 修复保留现有工作流及导出链路，仅增加整周期均衡求解、对应状态和回归。工时统计仍来自真实签到的人工汇总，转账仍只以工时配平。
- 个人目标继续作为人工草稿参考；输入了个人目标时不宣称全员等工时最优。有限搜索未确认最优时明确提示，保留人工调整入口。
- 用户希望 GitHub 可供他人下载、复现；README 要整合原有技术说明与作者提供的个人介绍，保留人味，不把它变成只有技术术语的文档。
- 公开网页采用 Pyodide 在浏览器执行共享 Python；无需为静态部署把计算核心改写成 JavaScript。依赖版本、校验文件、下载脚本、构建步骤和手动 Pages 工作流一并入库。
- 新增均衡回归与证据见《网页版本交付与回归记录.md》，实现边界见《工程交接说明.md》。AI 仍只参与开发协作，不承担运行时姓名核对、算术、硬约束校验或最终配平。
