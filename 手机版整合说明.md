# shukuwork 手机版整合说明

[打开工作台](https://libschedpay.bouncy-wren-3789.chatgpt.site) · [复现与部署](部署与GitHub说明.md) · [原版迭代记录](legacy-js/README.md)

这次根据 shukuwork 仓库中的界面代码调整现有工作台。没有取得 mcode 部署页面的实际画面，因此按源码实现主要布局与手机使用细节，保留已经验证的排班、工时配平和 Excel 核心。

## 界面变化

- 电脑使用左侧导航；手机使用顶部固定导航，随时切换“排班”和“工时统计与转账”。
- 背景由浅蓝过渡到浅粉，按钮和卡片沿用同一套配色。
- 手机上的配平明细保留“姓名、实际、下发、差值”四列，隐藏冗余状态列。差值使用 22px 粗体；负数标红，正数显示加号，零保留普通颜色。
- 工时输入框使用数字键盘提示与至少 16px 字号；手机聚焦输入框时将其移入视野。常用操作按钮在手机上纵向排列。
- “配平并输出转账表”标题旁增加“返回修改下发工时”；原来的底部返回按钮继续保留。返回后已填的名单、实际工时和下发工时继续保留，重新计算后更新配平草案。

## 版本关系与计算边界

当前入口源文件是 `workbench/static/index.html`，构建后的发布入口是 `dist/index.html`。
JavaScript 负责网页交互；`src/book_workbench` 中的固定 Python 代码负责姓名校验、算术、排班约束、配平与 Excel 输出，通过 Pyodide 在浏览器内运行。

本次没有修改计算核心，也没有让 AI 参与运行时计算。实际工时仍独立于排班，以负责人录入的签到统计为准；工时单位不涉及金额。现有代码支持小数工时，不会静默截断上传的小数。

GitHub 的 `legacy-js/` 原样保留 shukuwork 提交 `5fcd5bab90afc34f20ceebac79660e5dcd43f81b` 的八个文件，包括 v1–v5 的迭代 README。那里写的部署路径、算法一致性和测试结论属于当时版本，不能作为当前版本的说明。当前构建不引用该目录。

保留现有核心也保留了已经补上的保障：整周期均衡排班、准确识别实际工时列、小数精度、导出前重新校验配平，以及正确的 Excel 汇总公式。详细案例见 [回归记录](网页版本交付与回归记录.md)。

## 本次验证

- Python 自动测试 41 项通过。
- JavaScript 规则与浏览器资源路径检查 8 项通过。
- 构建产物中的浏览器 Python 运行组件测试 36 项通过，覆盖排班、导入、配平和 Excel 往返读取。
- 源码交互检查确认差值正负显示、顶部/底部返回入口和返回后的数据保留；HTML 检查确认按钮 ID 唯一、资源存在、发布样式与源文件一致。

以上是代码和计算组件检查，尚未做真实手机浏览器的视觉验收。

## 继续修改

样式主要在 `workbench/static/style.css`，表格与步骤交互在 `workbench/static/app.js`。
修改后重新运行 `python3 scripts/stage_browser.py` 生成 `dist`，再按部署指南发布。不要直接修改 `dist` 来代替源文件修改。
