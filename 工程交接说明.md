# 书库工作台工程交接说明

## 当前目标

以最终成品为中心，只服务两类可靠输出：排班表；转账表（实际工时一览、差值明细、人工转账配平、公示表）。基本交互：输入已确认数据 → 固定脚本计算 → 页面预览 → 人工修改 → 自动校验 → 输出 Excel。

## 当前已完成

- 本地网页工作台：`workbench/static/index.html`、`app.js`、`style.css`。
- 本地服务：`workbench/server.py`，默认地址 `http://127.0.0.1:8765`。
- 排班计算与校验：`src/book_workbench/schedule.py`；整周期均衡求解：`schedule_balance.py`。
- 工时差额、转账配平与校验：`src/book_workbench/settlement.py`。
- 不依赖 Node 或 openpyxl 的 Excel 导入/输出：`src/book_workbench/xlsx_export.py`。
- 30–40 人随机名单、逐人官方下发工时、外部未配平提醒、人工转账公示表。
- “选择输出位置”已经放在各个输出表格按钮旁边。
- 排班约束步骤默认显示人员 × 日期 × 班次的勾选表：勾选表示绝对不能排，空白表示可排；系统自动转换成内部约束。另保留高级文本格式和可复制给其他 AI 的“约束整理提示词”。
- 排班第 5 步支持两种自动统筹口径：全部班次总工时平均；按班岗时长分别平均（例如 2 小时班和 3 小时班分开平衡）。个人目标工时仍可留空或手动填写。
- 排班第 2 步支持上传上一张成品排班表，提取班次时间、每班人数和人员名单作为新周期起点；旧表的具体人员安排不会复制；某天人数不同于默认人数时，会保存为每日人数例外。
- `tests/` 当前 41 项 Python 自动测试覆盖核心计算、整周期均衡、导出链路和远程安全模式；另有 8 项 JavaScript 测试。
- 正式导出前由后端重新计算和校验，不再信任网页中可能过期的结果；重复导出不会覆盖上一份成品。
- 排班 Excel 支持每日人数高于默认值以及任意数量的班次汇总；部分人员填写目标工时时，未填写者继续使用平均目标，不再被当成0小时。
- 文件上传不再依赖 Python 3.13 已移除的 `cgi` 模块，且限制单次上传20MB、用后删除临时文件。
- Excel 输出提供浏览器下载链接，为后续部署到支持 Python 的服务器保留出口。

## 当前约束与待扩展项

当前最可靠的硬约束录入方式是网页中的勾选表。内部仍兼容以下高级文本格式：

```text
姓名|YYYY-MM-DD|班次编号
```

例如：

```text
同学甲|2026-08-18|afternoon
同学乙|2026-08-18|afternoon
同学丙|2026-08-19|morning
同学丙|2026-08-19|afternoon
```

自然语言中的“只排晚班、只能在某几天、集中同一天、优先晚班、尽量和某人一起、为了均衡少排”等规则，仍不直接进入计算器。它们要么由负责人在勾选表中展开为明确的不可排格子，要么保留为软偏好/待确认后在排班草稿中人工调整。

## 继续扩展时的设计原则

先不要继续添加代码，先把约束分成四类，并确定每类如何确认：

1. 班次定义：早班 09:00-11:00 2h；下午班 14:00-16:00 2h；晚班 19:00-22:00 3h。人数是独立字段，由本次输入确定。
2. 硬约束：违反就不能输出，例如某天某班不能排、只允许晚班、只允许周五至周日。
3. 软偏好：尽量满足但不能满足时要显示，例如优先晚班、尽量和某人一起、集中同一天。
4. 分配方案/均衡目标：由负责人确认，例如计划总工时、谁可以少排、空岗如何处理；AI不能自行猜测。

用户说的“我想到一个可能方案”不自动等于规则。只有用户明确确认过的内容才进入正式计算；不确定的想法必须保留为待确认项。

## 新窗口继续工程时的固定开场语

```text
继续书库工作台工程。请先读取 README.md、工程交接说明.md、业务需求与决策记录.md 和当前代码，先不要写代码。先复述真实工作流，将已复现问题映射到输入、固定计算、人工修改、自动校验或 Excel 输出层，再提出最小修复集和对应测试。
```

## 测试命令

```bash
python3 -m unittest discover -s tests -v
python3 scripts/verify_xlsx.py outputs/*.xlsx
```


## 2026-09-05 网页版本补充（优先于旧描述）

用户再次确认：晚班 19:00–22:00；转账只使用工时，不涉及金额；
实际工时来自人工统计的签到，不依赖排班或缺岗；官方下发只有本次上限约束，
每个人可以分别填写。实际工时统计表在进入下发步骤前可独立输出。

原“服务端重新校验”是为了防止旧预览或人工编辑绕过校验，并非必须部署 Python HTTP 服务。
公开版由浏览器 Worker 内的原 Python 代码在导出前重新校验；本地服务沿用同一导出边界。
没有引入由 AI 承担姓名匹配、算术、硬约束或最终配平的设计。

修复范围、四项问题的五层映射和回归证据见《网页版本交付与回归记录.md》。


GitHub 复现入口已补充：`scripts/download_pyodide.py` → `scripts/stage_browser.py`；详见《部署与GitHub说明.md》。该部署适配复用 Python 核心；下述均衡修复是单独的业务算法变更。

## 2026-09-05 整周期均衡修复（优先于旧分配描述）

用户再次明确：两种口径均先追求总工时均衡；第二种再尽量均衡各时长次数，早班与下午班同为 2h，归为同一组。

旧算法逐班贪心，缺少全周期重新调配。35 人、245h 的回归输入在存在硬约束时排出 33 人 7h、1 人 4h、1 人 10h；同一输入存在全部 7h 的有效安排。

最小修复保留旧草稿生成器，在其后增加确定性配额搜索与带上下界的二分图流检查。按工时区间宽度从小到大检查可行性，使用完整岗位，不拆班、不放松不可排、不减少已填岗位。第二口径额外优先尝试每类次数达到平均数的向下/向上取整范围。

边界：个人目标沿用旧草稿策略；超过 4 种正时长、岗位格数乘人数超过 20000、候选组合过多或搜索节点超过 2000 时保留草稿并明确返回 `search_limit`。只有完成搜索才报告已验证的最小工时差；不声称复杂输入一定能求出最优，也不保证受限时各时长次数达到全局次级最优。

网页增加最低、最高工时与最大差距；人工编辑后撤销最优状态，要求重新校验。Excel 沿用既有导出边界重新计算汇总。实际工时和转账模块的业务逻辑保持原状。

`tests/test_schedule_balance.py` 包含 7 项回归：两个模式的有约束等工时、无约束总工时、2h 合组、独立穷举验证最小差距、确实不能等工时、预算耗尽、Excel 汇总一致。随站点发布的 WebAssembly Python 共运行 36 项测试。
