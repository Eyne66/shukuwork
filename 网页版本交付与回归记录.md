# 网页版本交付与回归记录

基于 Eyne66/LibSchedPay main 的既有实现与三份交接记录继续修改，日期：2026-09-05。

## 三条工作流

1. 排班：确认周期 → 每日各班次的人数与时间 → 本期名单 → 人工确认不可排规则 → 固定代码计算总岗次、工时及平均份额并生成草稿 → 人工修改 → 重新校验 → 排班 Excel（包括每人工时和岗位需求）。晚班默认 19:00–22:00。
2. 工时统计：确认签到统计时段 → 本期名单 → 按名单顺序填入已人工汇总的实际工时，或按姓名导入完整表 → 行数、姓名和非负数校验 → 修改 → 单独输出实际工时统计表。无需先有排班表或下发上限。
3. 工时转账：沿用实际工时 → 本次上限 → 逐人确认官方下发 → 计算实际减下发的差额 → 同额优先再匹配剩余差额 → 人工编辑转出人、转入人、工时 → 逐人及全局校验 → 正式 Excel。未配平时允许草案与返回调整，保留已录数字。无金额换算，无自动补贴。

## 四处复现问题与五层映射

| 问题 | 输入 | 固定计算 | 人工修改 | 自动校验 | Excel 输出 |
| --- | --- | --- | --- | --- | --- |
| R1 下发列被认作实际列 | 列顺序影响字段识别 | 实际值被替换，差额随之失真 | 上传后原值不可信 | 原校验无法发现字段来源错误 | 错误差额可能被正式输出 |
| R2 姓名空格绕过同班重复检查 | 小王与小 王同时出现 | 同一个人重复占位、累计工时 | 手改姓名可引入 | 原重复检查用原始字符串 | 原明细又按原始名匹配，汇总不一致 |
| R3 跨周规则只保留最后一个星期 | 问卷含周一、周期超过一周 | 前一周禁排未进入计算 | 需人工逐日发现 | 原校验只能看到残缺规则 | 前一周可能输出不合实际限制的安排 |
| R4 名单外约束被静默丢弃 | 不可排规则的姓名与名单不符 | 前端过滤后无此规则 | 拼写或改名单可触发 | Python 未收到原规则，无法拒绝 | 出现表面校验通过的错误安排 |

## 最小修复

- R1：明确表头别名，拒绝重复或歧义列，缺实际列报错；保留无表头双列 Excel 兼容。
- R2：只做既有空白归一化，不猜姓名；排班输入、重复检查、导出记录使用同一名单姓名。
- R3：星期映射到周期内所有匹配日期，周天等同周日。
- R4：完整规则传入计算边界，名单/日期/班次核对后才编译到人员；前端也明确报错，保留无法显示在勾选网格里的原规则供修改。
- 网页适配：提取既有文件导入和导出校验函数为共享模块；保留 Python 排班、Decimal 配平和标准库 Excel 实现，增加浏览器内存文件适配。未重写算法或迁移到 AI 推理。
- 必要交互：每日人数表格、晚班默认时间、先统计后下发、修改返回保留数值、零差额无需转账也可正式导出；Excel 显示时段并附岗位需求。

## 首次网页交付回归结果

- `python -m unittest discover -s tests -v`：34 项通过，包括原 24 项。
- `node --test tests/test_ui_rules.cjs`：6 项通过，覆盖跨周星期、完整约束、姓名对应、行数和返回保留。
- `node scripts/test_pyodide.mjs`：在随网页发布的 WebAssembly Python 中运行 29 项通过；覆盖原核心 19 项与新增网页回归 10 项。
- 已生成并读回 Excel 核对规范姓名工时、19–22 时段、每日人数、实际统计合计、转入/转出正负号、未配平拦截和修改后配平。
- JavaScript 语法及本地静态文件引用检查通过。未进行浏览器交互测试，不将运行时测试宣称为浏览器端到端测试。

## 使用边界

- 每一次计算和导出都由固定代码处理，AI 不参与运行。
- 首次网页交付沿用贪心排班，之后按用户反馈增加整周期均衡求解，见下方修复记录。仍允许同日不同班次，缺岗仍提示并记录。
- 转账为同额优先的贪心配对，减少拆分但不保证全局最少笔数。若实际总工时超过人数乘上限，内部转账不能凭空解决总量不足，会提示修改或保留草案。
- 本版本无跨设备同步；刷新或关闭页面会清空当前工时与排班草稿，离开前下载成品。阶段名单可手动保存在本浏览器。
- 公开页面不含真实名单、签到或下发数据；用户输入仅在当前浏览器处理。


## 2026-09-05 GitHub 复现补充

用户已实际试用公开网页并反馈正常。随后把相同计算代码整理为可独立搭建的源码：

- 新增固定版本下载脚本与 SHA-256 锁定文件，不要求继承原作者的运行缓存。
- 网页脚本和资源路径兼容站点根目录及 `/LibSchedPay/` 子目录。
- 新增 GitHub Actions 回归工作流和手动触发的 GitHub Pages 工作流。
- 公开仓库提交范围排除原作者的 Sites 项目身份、运行缓存、实际输入和 Excel 输出。
- 更新 README 与部署指南，纠正旧文档对 GitHub Pages 的过期描述。

验证使用一份没有 `dist` 和个人托管配置的全新源码副本，从网络下载了全部六个锁定文件并核验哈希，成功构建静态站点。
Python 34 项、JavaScript 8 项、网页 WebAssembly Python 29 项测试通过；两类部署路径的入口引用和 JavaScript 语法检查通过。
这不等同于已经在仓库所有者账号启用 GitHub Pages；其工作流需按部署指南手动启动。

## 2026-09-05 用户试用后的均衡修复与 README 整合

### 复现与最小修改

35 人、7 天，每天早班 2h × 5 人、下午 2h × 5 人、晚班 3h × 5 人，共 245h，平均 7h。

- 旧总工时口径在无约束时得到 15 人 7h、10 人 6h、10 人 8h。
- 旧按时长口径加入部分不可排后，得到 33 人 7h、1 人 4h、1 人 10h。
- `tests/fixtures/schedule_fairness.json` 保留该输入及满足相同禁排条件、全员 7h 的独立可行安排。这是虚构名单的回归案例，不是用户真实数据。

| 层 | 本次修改 | 验证 |
| --- | --- | --- |
| 输入 | 保留字段，明确两种口径都优先总工时；第二种把同为 2h 的班次合组 | 原输入规则测试；2h/3h 次数测试 |
| 固定计算 | 旧草稿后增加整周期配额搜索和流可行性检查 | 两个模式均为 35 人 7h；小案例独立穷举验证最小差距 |
| 人工修改 | 保留草稿编辑；编辑后撤销最优标记 | 复用重新校验与导出边界 |
| 自动校验 | 显示最低、最高与差距；搜索未完成时明确提示 | 不可等工时与预算耗尽测试；既有硬约束回归 |
| Excel 输出 | 沿用共享导出，不改模板 | 生成后读回，35 人各为 7h |

有明确个人目标时不启用全员等工时求解；复杂输入在预算用尽后保留草稿，不把未找到最优误报为不可能。第二模式在最小总工时差内优先尝试各时长次数的向下/向上取整范围；不能满足时尽量优化，不保证次级目标全局最优。

### 当前验证结果

- Python：41 项通过，含新增 7 项均衡回归。
- JavaScript：8 项通过，含根目录和 GitHub Pages 项目子目录路径。
- 随网页发布的 WebAssembly Python：36 项通过，包含相同均衡案例及 Excel 读回。
- 未进行浏览器交互端到端测试；这些结果只代表所列计算、输入、导出和构建验证。

README 将作者的真实经历、使用想法、幽默和项目定位与原有输入方式、本地运行、部署、测试及英文说明整合。增加完整源码下载入口，并明确不必将 Python 计算核心改写为 JavaScript。
